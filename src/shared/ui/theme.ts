import { createTheme } from "@mantine/core";

export const appTheme = createTheme({
  primaryColor: "indigo",
  fontFamily: "Inter, system-ui, Arial, sans-serif",
  headings: { fontFamily: "Inter, system-ui, Arial, sans-serif" },
  defaultRadius: "md",
});
